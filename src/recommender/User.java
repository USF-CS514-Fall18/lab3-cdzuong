package recommender;

import java.util.Map;

public class User {
    double r1;
    double rMax;

    public int rValue(int compare) {
        int userMatch = 0;

        RatingsCollection user = new RatingsCollection();
        Map comparer = user.getRatingsMap();

        return userMatch;
    }
}

